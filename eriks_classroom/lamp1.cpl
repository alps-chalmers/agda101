integer x, y;
a: x := 0;
b: cobegin
    c: y := 0;
    d: cobegin
        e: y := 2*y
       and
        f: y := y + 3
       coend
   and
    g: while y = 0 do
         h: x := x + 1
       od
   coend;
j: x := 2 * y
